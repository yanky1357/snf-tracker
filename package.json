{
  "name": "reefpilot",
  "version": "1.0.0",
  "description": "AI-Powered Reef Tank Management",
  "private": true,
  "scripts": {
    "cap:init": "npx cap sync",
    "cap:open": "npx cap open ios",
    "cap:build": "npx cap copy ios && npx cap sync ios",
    "cap:run": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
